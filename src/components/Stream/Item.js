import React from 'react'

const Item = ({data}) => {

        const { Name, Tagline, Description, Published, Link } = data

    return (
        <ul>
            <li className="list-items">
                test
            </li>
        </ul>
    )
}

export default Item
